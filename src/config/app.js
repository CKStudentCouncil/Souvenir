/** When true, orders persist to localStorage instead of Firestore */
export const USE_MOCK_ORDERS = true

/** When true with mock orders, /admin is reachable without Firebase admin login */
export const MOCK_ALLOW_ADMIN_WITHOUT_AUTH = true
